
export interface DailyContent {
  activity: string;
  quote: string;
  author: string;
  source: string;
}

export interface LunarInfo {
  weekday: string;
  monthInWords: string;
  lunarMonth: string;
  lunarDay: string;
}

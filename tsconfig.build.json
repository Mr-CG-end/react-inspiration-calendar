{
  "extends": "./tsconfig.json",
  "compilerOptions": {
    "noEmit": false, //允许输出类型文件，因为build默认是不生成的
    "declaration": true, // 生成.d.ts文件
    "emitDeclarationOnly": true, // 只生成类型文件
    "outDir": "dist" // 生成位置
  },
  "include": ["src"] // 只扫描src
}
